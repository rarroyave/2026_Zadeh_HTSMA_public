numpy>=1.24
pandas>=2.0
scipy>=1.10
openpyxl>=3.1
sympy>=1.12
matplotlib>=3.7
pytest>=7

# Required transitively by the vendored PCM and TSM helper.py modules
CBFV>=1.1
HEACalculator>=2.0

# CBFV 1.1 imports pkg_resources, which setuptools>=81 removed. Pin below.
setuptools<81

# Notebook (optional; only needed for notebooks/reproduce_manuscript_figures.ipynb)
jupyter>=1.0
